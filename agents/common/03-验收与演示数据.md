# 验收与演示数据 v2.0

每个成员用本文的预期结果验证自己的实现。本文定义场景，执行状态初始均为“未执行”；任何成功率、耗时与截图必须由实际运行产生。

## 1. 统一数据集

由测试经理编写 `test-data/demo.json` 和 `test-data/scenarios/*.json`，配置经理实现开发环境导入。ID统一使用 `00000000-0000-0000-0000-XXXXXXXXXXXX`，下表尾号左补零到12位。不要直接使用团队学号作为软件测试账号。

| 类型 | 尾号/用户名 | 定义 |
|---|---|---|
| 组织 | 1001 / 1002 | 软件学院、其他学院 |
| 用户 | 2001 organizer_a | ORGANIZER，组织1001；活动A所有者 |
| 用户 | 2002 organizer_b | ORGANIZER，组织1001；活动B所有者 |
| 用户 | 2003 teacher_a | TEACHER，组织1001 |
| 用户 | 2004 resource_admin | RESOURCE_ADMIN，组织1001，管理校园全部资源 |
| 用户 | 2005 student_a | STUDENT，组织1001；已报名活动A |
| 用户 | 2006 staff_a | STUDENT，组织1001；活动A工作人员，需确认改期 |
| 用户 | 2007 sys_admin | SYS_ADMIN，组织1001，无业务审批权 |
| 用户 | 2008 teacher_other | TEACHER，组织1002，越权测试 |
| 用户 | 2009 student_b | STUDENT，组织1001，竞争名额测试 |
| 场地 | 3101 | 学术报告厅A，容量80，08:00—22:00 |
| 场地 | 3102 | 多功能厅B，容量120，08:00—22:00 |
| 器材 | 3201 | 投影设备组A，category=PROJECTOR，库存2 |
| 器材 | 3202 | 投影设备组B，category=PROJECTOR，库存2 |
| 活动 | 4001 | 活动A“校园技术分享会”，OPEN，version1，expectedAttendees60、registrationCapacity20，staff=[2006] |
| 活动 | 4002 | 活动B“社团交流会”，SCHEDULED，version1，owner2002，expectedAttendees50、registrationCapacity20，staff=[] |

演示日期D为首次导入当天上海日期+7天，可用DEMO_DATE覆盖。自动测试固定Clock=`2030-10-19T00:00:00Z`、D=`2030-10-20`，严禁测试依赖实际当天。生产Clock使用系统时间，测试不能修改全局系统时钟。

- A原安排：D日10:00—11:00，布撤场各30分钟，场地3101、器材3201数量2；占用09:30—11:30；2005已报名。
- B安排：D日14:30—16:30，布撤场0，场地3101、器材3201数量1。
- A改期目标：D日15:00—16:00，布撤场各30分钟，仍3101+3201×2。应同时报场地冲突、器材缺1。
- 首选替代方案：目标时间不变，改为3102+3202×2；代价元组前3项为(0,1,1)。所有其他规则相同。
- 示例密码通过本地DEMO_PASSWORD环境变量供种子程序BCrypt编码。任务包不包含真实密码，发布说明标出演示账号，避免公开使用开发口令。

`demo.json`顶层固定为 `{schemaVersion:1, organizations:[], users:[], resources:[], activities:[], registrations:[]}`。用户项含id/username/displayName/organizationId/roles；密码不写入JSON。活动项含id/ownerId/organizationId/status/versionNo/input，日期用`${DEMO_DATE}`占位，由导入器替换；资源结构采用ResourceView。导入器仅在dev profile且显式 `APP_DEMO_SEED=true` 时运行，已存在则校验/跳过，不覆盖用户已修改数据；重新演示使用新数据库或明确授权的重置流程。

## 2. 验收场景与实际断言

编号D-AT用于本开发基线，不覆盖旧讨论稿的AT编号。测试经理维护映射。

| 编号 | 操作与数据 | 必须观察到的结果 | 主责 |
|---|---|---|---|
| D-AT-01 | organizer_a登录；错误密码登录；停用账号后带旧令牌请求 | 正常200；错误401；停用后401；响应无passwordHash | PM |
| D-AT-02 | teacher_other审批A；sys_admin审批；organizer_b编辑A | 均403，活动、预约及审批记录无成功变化 | PM+AN |
| D-AT-03 | 新建→编辑→提交→教师同意→资源确认→发布 | 状态及版本按基线；首次确认前0条ACTIVE预约；确认后完整数量 | AN |
| D-AT-04 | 同场地两场活动，相邻占用分别[10:00,11:00)、[11:00,12:00) | 可共存；若后场布场15分钟则冲突[10:45,11:00) | DS |
| D-AT-05 | 器材库存2，旧预约[10,11)×1及[11,12)×1，新申请[10,12)×1 | 成功，任一时刻恰2；不能误算为3 | DS |
| D-AT-06 | 库存2，旧[10,12)×2，新[11,13)×1 | 失败，指出[11,12)缺1；其他资源无新增占用 | DS |
| D-AT-07 | 两个已教师批准的活动同一场地同一时段，2个请求同步释放起跑栅栏 | 恰1成功、1资源冲突；每资源不超额；失败活动无部分预约 | DS+AN |
| D-AT-08 | 同key同body重复确认；同key不同body再请求 | 第1种返回相同状态/实体且不新增记录；第2种409 IDEMPOTENCY_KEY_REUSED | PM |
| D-AT-09 | A改到15:00，查看冲突与替代方案 | 两类冲突正确；首选为3102+3202×2同时间；预览未改数据库 | DS+UI |
| D-AT-10 | A选择首选并创建改期，staff_a同意，teacher_a同意，resource_admin同意 | 状态依次正确；A变version2；旧预约RELEASED，新预约ACTIVE；报名保留且version2；通知/审计关联version2 | CM+AN |
| D-AT-11 | 工作人员或教师拒绝改期 | REJECTED；A原版本/预约完全保留；报名冻结解除 | CM |
| D-AT-12 | 预览可行后，另一活动先占候选资源，再最终确认 | 409 RESOURCE_CONFLICT，改期CONFLICTED；A原有效安排保留；无新预约 | CM+DS |
| D-AT-13 | 改期目标与自身旧占用部分重叠，其他活动无冲突 | 排除自己的旧占用后正确生效，不把自己算作额外竞争活动 | DS+CM |
| D-AT-14 | 在activatePlan之后、预约替换中注入RuntimeException | 整个事务回滚：活动版本指针、预约、改期、outbox均恢复；无部分提交 | CM |
| D-AT-15 | 待处理改期时报名或退出；终态后重试 | 待处理时409 CHANGE_PENDING；终态后按名额规则执行 | AN+CM |
| D-AT-16 | 容量1的OPEN活动，20个启用学生并发报名 | 恰1个ACTIVE，其他CAPACITY_FULL；重复本人JOIN不增加人数 | AN |
| D-AT-17 | 已预约活动取消两次，取消同时与改期资源确认竞争 | 最终CANCELLED、0条ACTIVE预约、0个有效待处理改期；重复不多释放；不得复活 | AN+CM |
| D-AT-18 | 为A原占用资源创建重叠故障，随后让改期失败 | 原记录仍在但resourceRisk=AT_RISK；受影响清单有A；页面不能显示原安排可正常使用 | DS+UI |
| D-AT-19 | 通知消费者首次失败、随后恢复，并重复消费同event | 业务已提交；最终每收件人恰1通知；读取通知不改变工作人员确认 | CM |
| D-AT-20 | 活动被新版本替换后，旧baseVersion或expectedLockVersion提交 | 409 STALE_VERSION，无覆盖最新安排 | AN+CM |
| D-AT-21 | 普通学生查看日历、其他报名名单及别人通知 | 日历只显示无权限占用信息；名单与别人通知403；无个人信息泄露 | 全员 |
| D-AT-22 | 300候选以内对比first-fit与排序策略 | 相同数据/候选/约束；记录成功数、代价和耗时；无可行方案时明确范围 | DS+测试 |
| D-AT-23 | 按UI主线操作且刷新浏览器、重启后端 | 状态来自真实持久化数据；页面正常恢复，无仅内存中的成功结果 | UI+配置 |
| D-AT-24 | 干净环境按发布说明启动、换角色完成答辩 | 真实后端/数据库连通；无手改数据库才可继续的步骤；版本、手册、报告一致 | 全员 |

并发测试使用独立连接/事务、CountDownLatch或请求屏障，不是单连接循环。失败注入只能在测试上下文中替换bean/挂钩，不提供公网“制造故障”或“重置数据库”接口。

### 关键断言示例

```java
assertEquals(1, results.stream().filter(r -> r.httpStatus() == 200).count());
assertEquals(1, results.stream().filter(r -> r.httpStatus() == 409).count());
assertEquals(1, activeReservationsForVenueAndInterval(venueId, from, to));
// 回滚测试在异常发生前后读取数据库，不仅检查HTTP状态。
assertEquals(beforeEffectiveVersion, afterEffectiveVersion);
assertEquals(beforeActiveReservations, afterActiveReservations);
assertEquals(beforeOutboxCount, afterOutboxCount);
```

这些是断言含义示例；辅助查询由测试经理在DbTestSupport中实现。每个测试记录对应D-AT编号。

## 3. 统一运行与验证入口

以下命令是各负责人必须实现的执行入口，并非当前仓库已有工具。M0完成后命令应可用。默认在项目根目录PowerShell运行；Unix可用 `./server/mvnw` 替代 `.cmd`。

```powershell
. ./scripts/load-env.ps1
docker compose up -d db
./server/mvnw.cmd -f server/pom.xml test
./server/mvnw.cmd -f server/pom.xml verify
npm --prefix web ci
npm --prefix web run typecheck
npm --prefix web run test:unit -- --run
npm --prefix web run build
npm --prefix qa ci
npm --prefix qa exec playwright install chromium
npm --prefix qa run test:e2e
pwsh -File scripts/check.ps1 -Scope all
```

- 单元测试由Surefire运行 `*Test`；真实数据库及并发由Failsafe运行 `*IT`，`verify`不得配置跳过它们。数据库不可用应明确失败或报告阻塞，不能绿色通过。
- 本地 `TEST_DATABASE_URL` 指向 `campus_test`，另设用户名/密码。每次测试生成独立schema `t_<UUID去掉连字符>`，Flyway迁移后载入独立数据；schema名严格校验。禁止对campus_dev或外部库清表。
- E2E使用独立Compose测试项目和测试数据库，变量 `BASE_URL`；后端与数据库必须真实，只有前端单元测试允许Mock。默认Playwright Chromium，1366×768，必要页面另测1920×1080与390×844。
- `scripts/check.ps1`支持foundation、activity、scheduling、change、ui、qa、all，对应岗位文件中的测试集合；遇到缺失测试文件或0个测试必须失败，不能吞掉退出码。
- 整体成功依据为退出码0、预期测试数非0、无失败及必要人工UI核对。不得以“构建成功”代替业务正确。

## 4. 策略评价记录格式

测试经理实现 `qa/scripts/evaluate-scheduling.ts`，输入 `test-data/scenarios/scheduling.json`，输出 `artifacts/evaluation/results.csv` 和环境说明。字段：scenarioId、strategy、feasible、timeShiftMinutes、venueChanged、replacedEquipmentLines、evaluatedCount、elapsedMs。

DS提供相同候选生成器和约束检查器上的FIRST_FIT与RANKED策略入口，仅供测试/benchmark调用，不新增公共控制策略API。对同一环境每场景预热1次、测量10次，报告中位数及范围。数据至少包括纯时间冲突、换场地、单器材替换、无解、边界相接、数量峰值和故障。两者都成功且成功率相同也应如实报告。

## 5. 完成证据

每个岗位的handoff.md包含：commit或文件清单、基线版本、执行命令、实际通过/失败/阻塞、日志路径、已知问题、接收人及下一动作。截图放 `artifacts/screenshots/`，报告放 `artifacts/test-results/`，不得提交密码和真实账号资料。

最终答辩先演示D-AT-09/10的成功主线，再选D-AT-12或14解释原安排保护，最后展示故障影响、审计及实际测试报告。参与成员需能解释自己模块的数据和失败分支。
