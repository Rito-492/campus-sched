# 杨畅（17231018）：界面设计师与前端开发任务 v2.0

> 给执行agent：读取[总说明](../taskbook.md)及common契约，按UI-01至UI-06创建真实Vue应用、原型与用户手册。目标是可交互且精致的完整前端，不是几张静态截图。

技术：Vue3、TypeScript、Vite、Vue Router、Pinia、Element Plus。必须遵守[界面规范](common/05-界面规范.md)的颜色、间距、布局和关键交互；后端字段依[接口契约](common/02-数据与接口契约.md)。

## 1. 所有权与依赖

拥有 `web/`全部文件、`docs/ui/`、界面截图；web的package.json和锁文件只能由你统一维护。QA另有qa工程，不得互相覆盖构建配置。

消费PM账号/通用接口、AN活动审批/报名、DS资源日历/方案、CM变更/通知/审计。后端未到时在测试及明确的fixture开发模式实现UI；真实集成验收必须关闭fixture。

## UI-01：M0前端工程、设计变量与原型

**文件：** `web/package.json`、`package-lock.json`、`vite.config.ts`、`tsconfig*.json`、`index.html`；`src/main.ts`、`App.vue`、`router/index.ts`、`styles/tokens.css`、`styles/base.css`、`layouts/AppLayout.vue`；`docs/ui/prototype/`、`interaction-spec.md`。

- [ ] 建立Vue+TS项目并启用严格类型、Router/Pinia/Vitest；npm脚本固定dev、typecheck、test:unit、build，提交锁文件，Node版本写入环境说明。
- [ ] 实现224px侧栏、64px顶栏、内容区、折叠导航；所有公共样式使用tokens，不让页面各自发明颜色与间距。
- [ ] 先制作日历、冲突方案、变更差异、确认时间线的可查看低保真原型；Figma不可访问时交付本地可编辑SVG/HTML和说明，不伪造链接。
- [ ] 路由按公共UI表建立，未实现页面显示明确状态，不能显示假业务成功。
- [ ] 提供可独立审阅的组件fixture，显著标注未连接后端；M0交工程构建结果与原型给全员。

```powershell
npm --prefix web install
npm --prefix web run typecheck
npm --prefix web run build
```

首次生成锁文件后，其他成员和后续干净构建使用npm ci；不反复升级依赖。

## UI-02：API客户端、登录与权限导航

**文件：** `src/types/api.ts`、`api/http.ts`、`api/auth.ts`、`stores/session.ts`、`views/LoginView.vue`、`views/UserAdminView.vue`；`api/http.spec.ts`、`views/LoginView.spec.ts`。

- [ ] 按公共契约逐字段声明DTO，不能把任意响应全写成any；统一解包data及错误对象。
- [ ] 登录成功后token保存在当前标签页sessionStorage，页面刷新用auth/me校验；退出清除，401跳转登录；不将密码或token写日志。
- [ ] 写操作首次生成Idempotency-Key，超时重试复用原key和body；用户改变内容后创建新命令。409保留输入并重新获取当前状态。
- [ ] 根据角色展示导航和动作，后端仍负责真实授权。账号管理使用API-04—06/38；无权限页提供清楚反馈。
- [ ] 单元测试覆盖成功/401/403/409、错误信息和重试key，先写断言再实现。

## UI-03：活动、审批与报名

**文件：** `api/activities.ts`、`views/ActivityListView.vue`、`ActivityEditorView.vue`、`ActivityDetailView.vue`、`ReviewQueueView.vue`；`components/ActivityStatus.vue`、`ApprovalTimeline.vue`、`RegistrationPanel.vue`。

- [ ] 新活动流程先填写完整ActivityInput并POST保存DRAFT取得id，再使用schedule-preview检查；不要在没有activityId时调用需要id的接口。
- [ ] 支持草稿编辑/提交/撤回，教师审批，资源确认，开放报名及取消；每次写入携带最新lockVersion，成功后重新获取详情。
- [ ] 详情分清审批状态、资源状态、有效版本和风险；STUDENT看到自己的myRegistrationStatus，不能调用受限名单接口获取其他人。
- [ ] 待处理改期时显示报名冻结原因；容量满、重复提交、网络失败有对应反馈。
- [ ] ReviewQueue按角色加载scope=review及相关改期，限制并发6；只展示当前角色可执行动作。

## UI-04：资源日历、故障与方案对比

**文件：** `api/resources.ts`、`api/scheduling.ts`；`views/ResourceCalendarView.vue`、`ResourceManageView.vue`；`components/ResourceTimeline.vue`、`ConflictList.vue`、`AlternativeCard.vue`、`AlternativeCompare.vue`；对应组件spec文件。

- [ ] 日历实现08:00—22:00时间轴和资源行，SETUP/EVENT/TEARDOWN不同表达；器材聚合显示同时已用数量，FAULT层显示不可用数量。
- [ ] 点击块开明细抽屉；activityId=null时不提供私有活动链接，遵守后端脱敏。
- [ ] 资源管理员可新增资源和登记故障，展示受影响活动；库存/容量修改首版不提供假入口。
- [ ] 冲突按资源列区间、需求、可用数与原因；方案最多5个，展示时间偏移/场地变化/器材替换/排序依据。
- [ ] 实现两到三个方案对比，选中方案不会直接预约；按钮转入影响确认。
- [ ] 单元测试日历时间映射、相邻边界、0/5方案、长名称和无可行方案。

## UI-05：改期工作台、确认、通知与记录

**文件：** `api/changes.ts`、`api/notifications.ts`；`views/ChangeWorkbenchView.vue`、`ChangeDetailView.vue`、`NotificationView.vue`；`components/PlanDiff.vue`、`ImpactChecklist.vue`、`ChangeTimeline.vue`。

- [ ] 改期工作台顺序为输入目标→预览冲突/方案→选择→新目标重新预览影响→创建申请；提交的targetPlan必须等于用户最后确认的方案。
- [ ] 并列当前有效安排和目标，资源/审批/工作人员/报名影响分组；使用registeredCount，不用裁剪后的ID数组长度冒充人数。
- [ ] 根据ChangeStatus显示待确认、待教师、待资源、已生效、已拒绝、冲突失效、已撤回、因取消关闭；错误状态有下一步入口。
- [ ] 工作人员只能确认自己；教师/资源决定使用独立按钮和意见框；拒绝需要原因；通知已读不能推进审批。
- [ ] 生效后刷新活动版本与资源日历；失败保留原安排，原资源AT_RISK时同时展示风险，不写无条件“原安排安全可用”。
- [ ] 通知与审计显示活动及版本，采用轮询刷新或用户主动刷新；首版不另引入WebSocket。
- [ ] 为公共规范列出的data-testid提供稳定标识，交QA实施跨角色E2E。

## UI-06：真实联调、视觉验收与手册

- [ ] 接入全部实际API，关闭fixture后完成创建/首次预约/报名/改期成功与失败流程；刷新和重启后仍正确。
- [ ] 每页完成加载、空数据、错误重试、无权限、长文本状态；删除装饰性假按钮。
- [ ] 按规范保存五个核心桌面页面及移动详情/通知截图，逐一检查截字、溢出、弹窗、键盘焦点、对比信息及控制台错误。
- [ ] 更新 `docs/ui/user-manual.md`：各角色登录、创建、审批、预约、改期、确认、报名、故障和通知的真实步骤与截图，标明版本；不写未实现扩展功能。
- [ ] 更新 `docs/ui/handoff.md`：页面/API对应、实际构建/测试、截图、已知问题和后端依赖。

```powershell
npm --prefix web ci
npm --prefix web run typecheck
npm --prefix web run test:unit -- --run
npm --prefix web run build
pwsh -File scripts/check.ps1 -Scope ui
```

## 2. 完成标准与交接人

11类路由页面可操作，四个核心交互符合规范；类型/单元/构建检查通过；真实后端演示主线完成；约定视口无明显布局问题；原型、截图、手册与交付版本一致。

交冯玉琪：交互发现的业务歧义；交各后端负责人：具体接口差异及复现；交汤雨润：路由、testid、截图和真实运行方式；交杨瀚霖：web构建与部署需求；交冀项阳：演示主线、风险和完成范围。
