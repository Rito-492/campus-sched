# 统一行尾：文本文件仓库内一律 LF，避免跨平台 CRLF/LF 混乱
* text=auto eol=lf

# PowerShell 脚本保持 CRLF（Windows PowerShell 5.1 兼容；
# 注意：.ps1 含中文时需以 UTF-8 with BOM 保存，否则 Windows PowerShell 5.1 按 ANSI 读取会乱码）
*.ps1 text eol=crlf

# Windows 批处理
*.cmd text eol=crlf
*.bat text eol=crlf

# 二进制文件不做任何转换
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.xlsx binary
*.docx binary
*.doc binary
*.pdf binary
