# AGENTS.md — campus-sched 仓库约定

> 本文件是 AI agent 与新成员进入仓库的**第一个入口**，只回答三件事：权威资料在哪里、东西放哪里、哪些红线不能碰。
>
> 项目与开发任务的完整说明以**开发任务包 v2.0** 为唯一权威（§0）；本文件只补充任务包未覆盖的仓库治理约定。两者如有出入，以任务包为基准。
>
> 最后更新：2026-09-26

## 0. 唯一权威：开发任务包 v2.0

开发任务包位于仓库根目录，是一切开发任务的起点与唯一真相源：

| 文件 | 内容 |
|---|---|
| `taskbook.md` | 总开发任务书：产品边界、六人开发分工、M0–M4 依赖、交付物对照、启动指令 |
| `agents/common/01-开发基线.md` | 技术、范围、权限、状态机、业务及一致性规则 |
| `agents/common/02-数据与接口契约.md` | DTO、38 个 API、数据库模型、Java 端口 —— **跨模块唯一契约** |
| `agents/common/03-验收与演示数据.md` | 固定数据集、24 项验收场景（D-AT-01…24）、运行与验证入口 |
| `agents/common/04-协作与集成规程.md` | 模块/目录所有权、M0 基础交付、集成顺序、分支与交接 |
| `agents/common/05-界面规范.md` | 视觉变量、页面路由、四个核心交互及截图验收 |
| `agents/<学号>-<岗位>.md` | 六份个人任务书（PM/AN/DS/QA/CM/UI-xx 编号任务） |

**读法**（见 `taskbook.md` §8）：先读 `taskbook.md` 与 common 五份契约，再读对应成员的个人任务书；按任务编号实际编写代码、测试和课程文档，不要只复述计划。契约中的字段名、枚举、URL、状态机不得各自改名；契约变更走 02 契约 §7 的跨模块修改规程。

**开工步骤**（新成员/新 agent 从这里开始）：

1. `git clone` 本仓库后，切换到自己的分支：`git checkout work/<学号>-<模块>`（六个分支已预建，对应关系见 04-协作规程 §4）；
2. 用 `taskbook.md` §8 的启动指令向 agent 报明身份：【姓名、学号、岗位】；
3. agent 依 `taskbook.md` §3 定位你的开发模块与个人任务书，按任务编号实际执行；
4. 每项任务完成即运行规定检查，并更新 `docs/<岗位目录>/handoff.md`。

**合并 main 前走 PR 评审**，统一口径见 `docs/testing/pr-review-checklist.md`（必须改/建议改/放行三档；交叉评审，不自审）。

本文件与任务包的分工：`taskbook.md` 说"做什么、谁做、做成什么样"；本文件说"这个仓库怎么管"。项目概况、团队分工、交付物清单、业务规则、里程碑一概见任务包，此处不重复。

## 1. 仓库结构

```text
campus-sched/
├── AGENTS.md / README.md / .gitignore   # 仓库元文件（仅此三件）
├── taskbook.md                  # 开发任务包总说明（v2.0；原 agent.md，入库更名）
├── agents/                      # 任务包：common/ 公共契约 + 六份个人任务书（固定结构）
├── docs/
│   ├── course/                  # 课程下发的管理文档（只读，原文件名）
│   │   └── role-confirmations/  # 六份成员角色确认表（含上/下游交接关系）
│   ├── management/              # 初始/详细项目计划、决策记录、风险清单（项目经理）
│   ├── requirements/            # 需求规范、用例、业务规则、需求追踪（系统分析员）
│   ├── design/                  # 设计文档与图源（设计师）
│   ├── ui/                       # 原型（prototype/）、交互规范、用户手册（界面设计师）
│   ├── testing/                  # 测试计划、测试设计、测试报告（测试经理）
│   └── configuration/           # 配置管理计划、配置项索引、结构指南（配置经理）
├── server/                      # Java 21 / Spring Boot 后端（根包 com.campus.events）
├── web/                         # Vue 3 / TypeScript 前端
├── qa/                          # Playwright E2E 与契约检查工程
├── test-data/                   # 演示与测试数据（demo.json、scenarios/）
├── contracts/                   # openapi.yaml
├── infra/                       # db 初始化 SQL、Nginx 配置
├── scripts/                     # load-env / check / retry-notifications 等脚本
├── release/                     # 发布包、发布说明与 manifest
├── artifacts/                   # 验收证据：截图、测试结果、策略评测
└── models/                      # Drawio UML 源文件（按图类型分子目录）
```

目录详解、"放什么/不放什么"与"该放哪"路由表见 `docs/configuration/repo-structure.md`；生产代码与配置的所有权以 `agents/common/04-协作与集成规程.md` §1 为准。结构变更走配置变更流程并同步本节总览树。

## 2. 仓库治理约定（任务包未覆盖的部分）

### 语言与命名

- 文档、图表内容、提交说明一律使用**中文**；目录名、代码标识符、分支名用**英文小写 kebab-case**。
- 文件与目录命名细则以 `docs/configuration/repo-structure.md` §5 为唯一来源，不在此重复。
- 任务包（`taskbook.md`、`agents/**`）内容为开发基线：**不拆并、不擅改公共契约**；公共契约内容的修改走 02 契约 §7 规程，由项目经理组织一致更新。总说明原名 `agent.md`，因与 `AGENTS.md` 在大小写不敏感文件系统（Windows/macOS）上产生 clone 碰撞而更名，内容未改动。

### 文档与目录规则

- 文档按 `repo-structure.md` §4 路由表存放；不确定时先看同类文档放在哪，跟随现有模式。
- Drawio 图：`.drawio` 源文件放 `models/` 对应子目录，导出图片与源文件同名放一起；Mermaid 图直接内嵌设计文档。
- 二进制文件（docx/xlsx/drawio/png）无法行级 diff：修改已评审版本时**另存新版本文件**或在文档内维护版本记录，不要静默覆盖。
- 课程原始 Office 文档由成员按实际进展填写；不得把旧模板或外校制度当成本校事实。

### Git、基线与变更流程

- 本仓库同时是课程配置库：需求、设计、发布都要形成**基线**（tag，如 `baseline/requirements-v1.0`）并登记基线清单（配置经理执行）。
- 分支约定（04-协作规程 §4）：`main` 为可集成分支；成员分支固定为 `work/<学号>-<模块>`（如 `work/55230223-foundation`），缺陷修复在对应模块分支上进行。
- 提交信息格式：`feat|fix|docs|test|model: 摘要`；涉及缺陷时引用 GitHub Issue 编号，如 `fix: 修复并发预约重复提交 (#23)`。
- 每轮开始先获取最新基准；提交只含自己的改动；合并冲突由对应所有者核对，禁止一律选 ours/theirs。
- 已进入基线的文档，修改必须走配置经理的变更流程（变更申请→评审→更新基线），**不要直接覆盖或重写**。
- `.gitignore` 已排除项不手动提交：Office 临时文件（`~$*`）、`.DS_Store`、`.env`（含密码/令牌）、构建产物（`target/`、`node_modules/`、`dist/` 等）。

### 红线

- 不要改动 `docs/course/` 下文档的**内容**（课程规定的固定格式与名单）。
- 不要绕过变更流程改写他人负责的已评审文档；不要覆盖他人未提交的修改。
- **契约红线**（任务包共同规则在本仓库的重申）：共享接口、字段名、枚举不得私自改名；不另写第二套响应包装、认证或幂等机制；预约、改期、取消必须经统一端口，不越过端口直改数据库表；端口实现未到时替身只放测试代码，生产不得返回伪造成功；不用静态成功页面或 Mock 冒充业务完成。
- 本校具体制度未经访谈核实：按演示产品规则执行（01-开发基线），**不臆造规则、不伪造访谈记录**。
- 测试、评审、提交、统计数据必须真实产生后记录；未执行的检查不得写通过。

## 3. 当前状态与未决事项（截至 2026-09-26）

- ✅ 选题确定、角色分工完成、工具选型完成（见 `docs/course/`）
- ✅ 开发任务包 v2.0 并入仓库（`taskbook.md` + `agents/`），取代"首轮只写文档"的旧方案
- ✅ 技术栈确定（01-开发基线 §2）；仓库结构按任务包重组；GitHub 远程已关联（`Rito-492/campus-sched`）
- ⬜ M0 基础交付：六岗位并行（清单见 04-协作规程 §2），完成后项目进入 M1；**第一步是项目经理交付《初始软件项目计划》**（PM-01 要求编码前起草），并借此与老师核实交付物共 12 项还是 11 项（任务包口径含"配置库"，待与考核表核对）
- ⬜ 交付物状态与排期随《初始软件项目计划》维护（PM-01，`docs/management/initial-project-plan.md`，编写前以本节为过渡记录）
- ⬜ `baseline/init-v1.0` 需在《配置管理计划》的基线清单中补登（配置经理）
- ⬜ 学校实际制度待访谈核实；当前按演示产品规则执行
