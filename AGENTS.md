# AGENTS.md — campus-sched

> 本文件面向后续接手本仓库的 AI agent 与新成员。读完本文件，你应当能回答：这是什么项目、要交付什么、文件放在哪里、遵循哪些约定。
>
> 最后更新：2026-09-26（开发任务包 v2.0 已并入仓库，进入 M0 开发阶段）。

## 0. 开发任务包 v2.0（一切开发任务的起点）

仓库根目录的开发任务包是本项目的**开发基线**，任何开发工作从这里开始：

| 文件 | 内容 |
|---|---|
| `agent.md` | 总开发任务书：产品边界、六人开发分工、M0–M4 依赖、交付物对照、启动指令 |
| `agents/common/01-开发基线.md` | 技术、范围、权限、状态机、业务及一致性规则 |
| `agents/common/02-数据与接口契约.md` | DTO、38 个 API、数据库模型、Java 端口 —— **跨模块唯一契约** |
| `agents/common/03-验收与演示数据.md` | 固定数据集、24 项验收场景（D-AT-01…24）、运行与验证入口 |
| `agents/common/04-协作与集成规程.md` | 模块/目录所有权、M0 基础交付、集成顺序、分支与交接 |
| `agents/common/05-界面规范.md` | 视觉变量、页面路由、四个核心交互及截图验收 |
| `agents/<学号>-<岗位>.md` | 六份个人任务书（PM/AN/DS/QA/CM/UI-xx 编号任务） |

执行开发任务的固定读法（见 `agent.md` §8）：**先读 `agent.md` 与 common 五份契约，再读对应成员的个人任务书**，按任务编号实际编写代码、测试和课程文档，不要只复述计划。契约中的字段名、枚举、URL、状态机不得各自改名；契约变更走 02 契约 §7 的跨模块修改规程。

## 1. 项目概况

| 项 | 内容 |
|---|---|
| 项目名称 | campus-sched（面向校园活动的多资源协同调度与变更影响分析系统） |
| 项目性质 | 《软件工程案例分析与实践》课程团队项目：完整软件工程过程 + 可运行、可验证、界面精致的可交互系统 |
| 团队规模 | 6 人，六种课程岗位，每人同时承担明确的开发模块（见 §2 与 `agent.md` §3） |
| 仓库定位 | 配置库（GitHub）：管理文档、图稿、代码的版本、基线与发布 |

**一句话概括**：以校园讲座、竞赛、社团展演等活动为主线，将活动审批、场地与器材预约、人员协作和变更处理关联起来，帮助活动组织者与管理人员提高信息核对与协同处理效率的交互式应用。

**三大特色（核心卖点，设计与测试都围绕它们展开）**：

1. **多资源联合调度**：制定活动安排时联合检查场地容量、开放时段、器材数量以及布场/撤场时间。
2. **冲突解释与替代方案**：指出具体冲突资源、时段和不足数量，在候选范围内提供换时间、换场地或替换器材的可行方案，并按明确偏好排序。
3. **变更影响分析**：活动改期或调整资源前，展示新旧安排差异及受影响的资源、审批、工作人员与报名记录；变更失败时不破坏原预约。

## 2. 团队与角色

| 姓名 | 学号 | 课程岗位 | 开发模块（详见 `agent.md` §3） | 主要交付文档 |
|---|---|---|---|---|
| 冀项阳 | 55230223 | 项目经理 | 后端公共工程 `foundation/auth/contract`、账号权限、幂等执行器、OpenAPI、整体集成 | `docs/management/` |
| 冯玉琪 | 22220314 | 系统分析员 | `activity/registration`：活动、版本、教师审批、首次资源确认编排、发布取消、报名 | `docs/requirements/` |
| 黄方勇 | 55230315 | 设计师 | 数据库迁移、`resource/scheduling`：资源、故障、日历、约束检查、联合预约、替代方案 | `docs/design/` |
| 汤雨润 | 55230316 | 测试经理 | `qa/`、`test-data/`、跨模块并发/失败验收测试、E2E、策略评测 | `docs/testing/` |
| 杨瀚霖 | 55231031 | 配置经理 | `change/notification/audit/dev`：改期、人员确认、原子切换、outbox 通知、审计、环境及发布 | `docs/configuration/` |
| 杨畅 | 17231018 | 界面设计师 | `web/` 全部：Vue 前端页面与核心交互、API 接入、原型、用户手册 | `docs/ui/` |

⚠️ **三个易混淆概念**（文档中不要混用）：
1. 系统使用者角色（STUDENT / ORGANIZER / TEACHER / RESOURCE_ADMIN / SYS_ADMIN，见 §3）≠ 项目组课程岗位；
2. 课程岗位 ≠ 开发模块责任（本轮每人岗位之外承担具体开发模块，分工依据是 `agent.md` §3）；
3. 目录/代码所有权以 `agents/common/04-协作与集成规程.md` §1 的所有权表为准，未列明时由项目经理指定，不能默认归自己。

> 各岗位上/下游交接关系记录在 `docs/course/role-confirmations/` 角色确认表中；开发交接以各岗位 `docs/<岗位目录>/handoff.md` 为准（格式见 04-协作规程 §4）。
>
> 任务分解查询以 `agents/` 任务包为准；**交付物完成状态以 `docs/management/task-assignments.md` §0 为唯一状态源**（项目经理维护）。

## 3. 产品规则速览

**本校具体审批流程与资源管理规则尚未经访谈核实**。任务包已将其固化为**课程演示产品规则**（首次教师审批后资源确认；待审批不占资源；改期需工作人员确认→教师审批→资源最终确认；失败不破坏原安排；资源故障风险明确显示）。执行时以 `agents/common/01-开发基线.md` 为权威版本；不得把演示规则当成本校事实，也不得伪造访谈记录。真实制度核实后如需调整，走契约的跨模块修改规程统一变更。

**七大功能**（与任务包范围的对应见 01-开发基线 §1）：

1. 用户与权限管理 — 五类角色 × 管理范围（权限矩阵见 01-开发基线 §3）
2. 活动策划与审批 — 草稿→提交→教师审批→资源确认→发布；版本不可变、审批绑定版本、区分活动审批与资源确认
3. 多资源联合预约 — 场地同时刻独占 + 器材按数量池管理，占用区间含布撤场
4. 冲突解释与替代方案 — 五类冲突码（场地容量/开放时段外/场地占用/器材不足/资源故障）；±120 分钟固定搜索、300 候选预算、返回前 5 个可行方案
5. 变更影响分析与协同处理 — 影响预览、工作人员确认、重新审批、原子切换，全过程记录
6. 报名与人员协作 — 限额报名、退出、工作人员维护改期确认、站内通知
7. 资源状态与历史追溯 — 故障登记、受影响活动清单、审计日志

**首版明确不实现**（见 01-开发基线 §1）：候补递补、签到、器材归还验收、任务看板、统计报表；不接校园身份、短信、支付或硬件；不做自动人员排班，不承诺全局最优。

**五类使用者**：参与学生 / 活动组织者 / 审批教师 / 资源管理员 / 系统管理员；同一账号可兼具多角色，权限每次由后端校验。

**四个重点交互**（`agents/common/05-界面规范.md` §3）：资源日历、冲突与替代方案对比、变更差异与影响清单、确认与结果时间线。

## 4. 交付物与考核

课程考核交付物共 12 项及存放位置（与 `agent.md` §6 一致）：

| 编号 | 交付物 | 主责 | 输出位置 |
|---|---|---|---|
| 1 | 初始软件项目计划 | 冀项阳 | `docs/management/initial-project-plan.md`，编码前起草 |
| 2 | 软件需求规范 | 冯玉琪 | `docs/requirements/software-requirements.md` |
| 3 | 用户界面原型 | 杨畅 | `docs/ui/prototype/` 中可查看的原型 |
| 4 | 详细软件项目计划 | 冀项阳 | `docs/management/detailed-project-plan.md` |
| 5 | 软件测试计划 | 汤雨润 | `docs/testing/test-plan.md` |
| 6 | 配置管理计划 | 杨瀚霖 | `docs/configuration/configuration-management-plan.md` |
| 7 | 设计文档 | 黄方勇 | `docs/design/system-design.md` 及图源 |
| 8 | 软件测试设计 | 汤雨润 | `docs/testing/test-design.md` |
| 9 | 测试报告 | 汤雨润 | `docs/testing/test-report.md`，执行后形成 |
| 10 | 配置库 | 杨瀚霖（全员贡献） | 实际版本库；`docs/configuration/configuration-index.md` 仅作索引 |
| 11 | 交付产品 | 冀项阳统筹（全员开发） | 可运行发布包与 `release/README.md` |
| 12 | 用户手册 | 杨畅 | `docs/ui/user-manual.md` |

> 完成状态以 `docs/management/task-assignments.md` §0 为唯一状态源，本表不重复跟踪。
> 各岗位另有 `docs/<岗位目录>/handoff.md` 交接单（04-协作规程 §4 固定格式），不视为考核交付物但强制维护。

**考核规则速记**：项目组分数 = 5×过程评估 + 5×产品评估；成员分数 = 项目组分数 × 成员过程评估 × 角色权值 / 100；3 次缺席或未提交规定文档者取消考核资格。详见 `docs/course/项目考核评估表.doc`；具体执行口径、日期与个人权值由项目经理核实。

## 5. 工具链

| 用途 | 工具 | 与本仓库的关系 |
|---|---|---|
| 项目/任务管理 | 飞书（任务看板） | 仓库外；任务看板关联提交与问题 |
| 版本/配置管理 | GitHub（本仓库） | 全部文档、图稿、代码 |
| 缺陷管理 | GitHub Issues | 仓库内 Issue，提交信息引用编号 |
| 建模绘图 | Drawio（Mermaid 可内嵌文档） | 源文件 `.drawio` 存 `models/`，导出图片一并提交 |
| UI 设计 | Figma | 导出 png/pdf 到 `docs/ui/prototype/` |
| 运行/部署 | Docker Compose | db + server + web 三服务（04-协作规程 §5） |

## 6. 仓库结构

```text
campus-sched/
├── AGENTS.md / README.md / .gitignore   # 仓库元文件（仅此三件）
├── agent.md                     # 开发任务包总说明（v2.0，固定文件，不改名）
├── agents/                      # 任务包：common/ 公共契约 + 六份个人任务书（固定结构）
├── docs/
│   ├── course/                  # 课程下发的管理文档（只读，原文件名）
│   │   └── role-confirmations/  # 六份成员角色确认表（含上/下游交接关系）
│   ├── management/              # 初始/详细项目计划、任务分配与状态（项目经理）
│   ├── requirements/            # 需求规范、用例、业务规则、需求追踪（系统分析员）
│   ├── design/                   # 设计文档与图源（设计师）
│   ├── ui/                       # 原型（prototype/）、交互规范、用户手册（界面设计师）
│   ├── testing/                  # 测试计划、测试设计、测试报告（测试经理）
│   └── configuration/           # 配置管理计划、配置项索引、结构指南（配置经理）
├── server/                      # Java 21 / Spring Boot 后端（根包 com.campus.events）
├── web/                         # Vue 3 / TypeScript 前端
├── qa/                          # Playwright E2E 与契约检查工程
├── test-data/                   # 演示与测试数据（demo.json、scenarios/）
├── contracts/                   # openapi.yaml
├── infra/                       # db 初始化 SQL、Nginx 配置
├── scripts/                     # load-env / check / retry-notifications 等脚本
├── release/                     # 发布包、发布说明与 manifest
├── artifacts/                   # 验收证据：截图、测试结果、策略评测
└── models/                      # Drawio UML 源文件（按图类型分子目录）
```

> 各目录的**详细说明**（定位、放什么/不放什么、模块所有权、"该放哪"路由表）见 `docs/configuration/repo-structure.md`，由配置经理维护；生产代码与配置的所有权以 `agents/common/04-协作与集成规程.md` §1 为准。结构变更走变更流程并同步本节总览树。

## 7. 协作约定（agent 必读）

### 语言与命名

- 文档、图表内容、提交说明一律使用**中文**；目录名、代码标识符、分支名用**英文小写 kebab-case**。
- 文件与目录命名细则以 `docs/configuration/repo-structure.md` §5 为唯一来源，不在此重复。
- 任务包文件（`agent.md`、`agents/**`）是固定文件：**不改名、不拆并、不改公共契约内容**（契约修改走 02 契约 §7 规程）。

### 目录规则

- 文档按 §4/§6 的位置存放；不确定时先看同类文档放在哪，跟随现有模式。
- Drawio 图：`.drawio` 源文件放 `models/` 对应子目录，导出图片与源文件同名放一起。
- 二进制文件（docx/xlsx/drawio/png）无法行级 diff：修改已评审版本时**另存新版本文件**或在文档内维护版本记录，不要静默覆盖。
- 课程原始 Office 文档（`docs/course/`）由成员按实际进展填写；不得把旧模板或外校制度当成本校事实。

### Git 与基线

- 本仓库同时是配置库：需求、设计、发布都要形成**基线**（tag，如 `baseline/requirements-v1.0`）并登记基线清单。
- 分支约定（04-协作规程 §4）：`main` 为可集成分支；成员分支固定为 `work/<学号>-<模块>`（如 `work/55230223-foundation`），缺陷修复在对应模块分支上进行。
- 提交信息格式：`feat|fix|docs|test|model: 摘要`；涉及缺陷时引用 GitHub Issue 编号，如 `fix: 修复并发预约重复提交 (#23)`。
- 每轮开始先获取最新基准；提交只含自己的改动；合并冲突由对应所有者核对，禁止一律选 ours/theirs。
- 已进入基线的文档，修改必须走配置经理的变更流程（变更申请→评审→更新基线），**不要直接覆盖或重写**。
- 不提交 Office 临时文件（`~$*`）、`.DS_Store`、`.env`（含密码/令牌）、依赖目录与构建产物，`.gitignore` 已排除。

### 红线

- 不要改动 `docs/course/` 下文档的**内容**（课程规定的固定格式与名单）。
- 不要绕过变更流程改写他人负责的已评审文档；不要覆盖他人未提交的修改。
- **契约红线**：共享接口、字段名、枚举不得私自改名；不另写第二套响应包装、认证或幂等机制；预约、改期、取消必须经统一端口（`SchedulingPort` 等，见 02 契约 §5），不越过端口直改数据库表；端口实现未到时替身只放测试代码，生产不得返回伪造成功；不用静态成功页面或 Mock 冒充业务完成。
- 本校具体制度未经访谈核实：按演示产品规则执行（§3），**不臆造规则、不伪造访谈记录**。
- 测试、评审、提交、统计数据必须真实产生后记录；未执行的检查不得写通过（任务包共同规则）。

## 8. 常见任务指引

| 任务 | 做法 |
|---|---|
| 开始任何开发任务 | 读 `agent.md` + `agents/common/` 全部契约 + 本人任务书 → 按任务编号执行（启动指令见 `agent.md` §8） |
| 修改公共契约 | 走 02 契约 §7：写出影响的 API-ID、DTO、表、端口与调用方，项目经理组织一致更新 |
| 新增/修改课程交付文档 | 放所属目录（§4 路径），完成后更新 `task-assignments.md` §0 与飞书看板 |
| 画/改 UML 图 | 改 `models/` 下 `.drawio` 源文件，导出图片一起提交；Mermaid 图可直接内嵌设计文档 |
| 修缺陷 | GitHub Issue 登记 → 在对应模块 `work/` 分支修复 → 提交信息引用编号 → 回归后关闭 |
| 建基线 | 配置经理执行：打 tag `baseline/<阶段>-v<版本>` 并登记基线清单 |
| 查岗位任务/分工 | 读 `agents/` 个人任务书与 04-协作规程 §1；交付状态读 `docs/management/task-assignments.md` §0 |
| 启动开发环境 | 按 04-协作规程 §5：`load-env` 脚本 → `docker compose up -d db` → 后端 `spring-boot:run`（dev）→ 前端 `npm run dev` |
| 更新本文件 | 重大变化（契约升级、交付物完成、流程调整）时更新 §9 状态表与对应章节 |

## 9. 当前状态与未决事项（截至 2026-09-26）

- ✅ 选题确定、角色分工完成、工具选型完成（见 `docs/course/`）
- ✅ 开发任务包 v2.0 并入仓库（`agent.md` + `agents/`），取代"首轮只写文档"的旧方案
- ✅ 技术栈确定：Java 21 / Spring Boot 3.5.16 / Maven / PostgreSQL 17 后端；Vue 3 / TypeScript / Vite / Element Plus 前端；Docker Compose 部署（01-开发基线 §2）
- ✅ 仓库结构按任务包重组（`docs/management|ui|configuration` 等新布局 + `server/web/qa/test-data` 代码区骨架）
- ✅ git 仓库已初始化（`baseline/init-v1.0`），已关联 GitHub 远程（`Rito-492/campus-sched`）
- ⬜ M0 基础交付：六岗位并行（清单见 04-协作规程 §2），完成后项目进入 M1
- ⬜ `baseline/init-v1.0` 需在《配置管理计划》的基线清单中补登（配置经理）
- ⬜ 学校实际制度待访谈核实；当前按演示产品规则执行（§3）
- 交付物进度：以 `docs/management/task-assignments.md` §0 为唯一状态源
