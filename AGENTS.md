# AGENTS.md — campus-sched

> 本文件面向后续接手本仓库的 AI agent 与新成员。读完本文件，你应当能回答：这是什么项目、要交付什么、文件放在哪里、遵循哪些约定。
>
> 最后更新：2026-09-25（仓库初始化阶段，交付物均未开始）。

## 1. 项目概况

| 项 | 内容 |
|---|---|
| 项目名称 | campus-sched（面向校园活动的多资源协同调度与变更影响分析系统） |
| 项目性质 | 《软件工程案例分析与实践》课程团队项目：完整的软件工程过程实践（需求 → 设计 → 测试 → 配置管理）+ 可交互系统 |
| 团队规模 | 6 人，六种岗位分工（见 §2） |
| 仓库定位 | 配置库（GitHub）：管理文档、图稿、原型与代码的版本、基线与发布 |

**一句话概括**：以校园讲座、竞赛、社团展演等活动为主线，将活动审批、场地与器材预约、人员协作和变更处理关联起来，帮助活动组织者与管理人员提高信息核对与协同处理效率的交互式应用。

### 三大特色（核心卖点，设计与测试都应围绕它们展开）

1. **多资源联合调度**：制定活动安排时联合检查场地容量、开放时段、器材数量以及布场/撤场时间。
2. **冲突解释与替代方案**：指出具体冲突资源、时段和不足数量，在候选范围内提供换时间、换场地或替换器材的可行方案，并按明确偏好排序。
3. **变更影响分析**：活动改期或调整资源前，展示新旧安排差异及受影响的资源、审批、工作人员与报名记录；变更失败时不破坏原预约。

## 2. 团队与角色

| 姓名 | 学号 | 岗位 | 职责要点 | 主要交付文档 |
|---|---|---|---|---|
| 冀项阳 | 55230223 | 项目经理 | 项目计划、进度/范围/风险跟踪、阶段评审与交付督促 | 初始/详细软件项目计划 |
| 冯玉琪 | 22220314 | 系统分析员 | 需求调研、用例与补充规约、验收标准、需求追踪 | 软件需求规范、用例规约 |
| 黄方勇 | 55230315 | 设计师 | 子系统/类/接口/数据设计、用例实现与状态流转 | 设计文档 |
| 汤雨润 | 55230316 | 测试经理 | 测试计划/用例/环境/执行/缺陷跟踪/回归 | 测试计划、测试设计、测试报告 |
| 杨瀚霖 | 55231031 | 配置经理 | 配置管理规则、配置库、基线、变更、备份与发布 | 配置管理计划、配置库 |
| 杨畅 | 17231018 | 界面设计师 | 页面结构、导航与交互原型、界面规范、用户手册 | 界面原型、用户手册 |

⚠️ **易混淆点**：系统使用者角色（参与学生、活动组织者、审批教师、资源管理员、系统管理员，见 §3）与项目组岗位分工是两个不同概念，文档中不要混用。

> 每个岗位的上/下游交接关系（交接人、交接材料）记录在 `docs/course/role-confirmations/` 下各人的角色确认表中，调整流程或文档归属前先读它。

各岗位的阶段任务分解（谁在哪个阶段做什么、交付什么、放哪里）见 **`docs/planning/task-assignments.md`**，由项目经理维护；查询或更新任务分配以该文件为准，不要在本文件中复制任务清单。

## 3. 系统需求速览

以下摘自选题说明（`docs/course/项目组选题说明.docx`），作为背景知识；权威版本以后续 `docs/requirements/` 下的需求规范为准。

**七大功能**：

1. 用户与权限管理 — 按角色 × 管理范围控制活动修改、审批、资源管理等操作
2. 活动策划与审批 — 草稿→申请→批准/驳回/取消；保存申请版本和审批意见；区分活动审批结果与资源确认结果
3. 多资源联合预约 — 处理多活动竞争资源、重复提交与取消释放
4. 冲突解释与替代方案
5. 变更影响分析与协同处理 — 经必要确认后更新安排，保留全过程记录
6. 报名与人员协作 — 限额报名、退出、工作人员维护、站内通知
7. 资源状态与历史追溯 — 维护场地/器材可用状态，识别故障影响的后续预约

**扩展功能**（视开发周期与团队能力纳入详细计划）：签到、器材归还验收、候补递补、任务进度、统计。

**五类使用者**：参与学生 / 活动组织者 / 审批教师 / 资源管理员 / 系统管理员；同一账号可兼具多角色，但需明确授权。

**重点打磨的交互界面**：资源日历、冲突标注、替代方案对比、变更前后差异与影响清单。

**测试重点**（来自测试经理职责）：并发预约、重复提交、权限、取消释放、改期失败保护。

## 4. 交付物与考核

课程考核交付物共 11 项（文档评分 10 分制，建议 8–10 分）及存放位置：

| 交付物 | 存放位置 |
|---|---|
| 初始软件项目计划 | `docs/planning/` |
| 详细软件项目计划 | `docs/planning/` |
| 软件需求规范 | `docs/requirements/` |
| 用户界面原型 | `docs/prototypes/`（Figma 导出） |
| 软件测试计划 | `docs/testing/` |
| 配置管理计划 | `docs/configuration-management/` |
| 设计文档 | `docs/design/` |
| 软件测试设计 | `docs/testing/` |
| 测试报告 | `docs/testing/` |
| 交付产品（系统） | `src/`（源代码），发布包由配置经理管理 |
| 用户手册 | `docs/delivery/` |

> 交付物完成状态以 `docs/planning/task-assignments.md` §0 为唯一状态源（项目经理维护），本表不重复跟踪。

**考核规则速记**：项目组分数 = 5×过程评估 + 5×产品评估；成员分数 = 项目组分数 × 成员过程评估 × 角色权值 / 100；3 次缺席或未提交规定文档者取消考核资格。详见 `docs/course/项目考核评估表.doc`。

## 5. 工具链

| 用途 | 工具 | 与本仓库的关系 |
|---|---|---|
| 项目/任务管理 | 飞书（任务看板） | 仓库外；任务看板关联提交与问题 |
| 版本/配置管理 | GitHub（本仓库） | 全部文档、图稿、代码 |
| 建模绘图 | Drawio | 源文件 `.drawio` 存 `models/`，导出图片一并提交 |
| 缺陷管理 | Gitee Issue | 仓库外；缺陷编号引用到提交信息 |
| UI 设计 | Figma | 导出 png/pdf 到 `docs/prototypes/` |

## 6. 仓库结构

```text
campus-sched/
├── AGENTS.md                       # 本文件：agent / 新成员指南
├── README.md                       # 项目名片
├── docs/
│   ├── course/                     # 课程下发的管理文档（选题、分工、工具、考核）
│   │   └── role-confirmations/     # 六份成员角色确认表（含上/下游交接关系）
│   ├── planning/                   # 初始/详细项目计划、任务分解、风险清单
│   ├── requirements/               # 软件需求规范、用例规约、业务规则、需求追踪
│   ├── design/                      # 设计文档（子系统、类、接口、数据模型、用例实现）
│   ├── prototypes/                 # Figma 原型导出与原型评审记录
│   ├── testing/                    # 测试计划、测试设计、测试报告
│   ├── configuration-management/   # 配置管理计划、配置项/基线/变更记录
│   └── delivery/                   # 用户手册、发布说明等最终交付物
├── models/                         # Drawio 模型源文件（按图类型分子目录，按需创建；详见结构指南 §3.2）
│   ├── use-case/                   # 用例图（已建）
│   └── activity/                   # 活动图（已建；其余图类型目录待首次产出时创建）
├── src/                            # 系统源代码（技术栈待定）
├── tests/                          # 测试代码与测试数据
└── scripts/                        # 辅助脚本（构建、导出、校验等）
```

> 各目录的**详细说明**（定位、放什么/不放什么、命名示例、"该放哪"路由表）见 `docs/configuration-management/repo-structure.md`，由配置经理维护；结构变更须走变更流程并同步本节总览树。放文件前先查该指南的路由表。

## 7. 协作约定（agent 必读）

### 语言与命名

- 文档、图表内容、提交说明一律使用**中文**；目录名、代码标识符、分支名用**英文小写 kebab-case**。
- 文件与目录命名细则（课程下发文档保留原文件名、版本化命名示例、原型导出与 Drawio 命名）以 `docs/configuration-management/repo-structure.md` §5 为唯一来源，不在此重复。

### 目录规则

- 文档按 §4/§6 的位置存放；不确定时先看同类文档放在哪，跟随现有模式。
- Drawio 图：`.drawio` 源文件放 `models/` 对应子目录，导出图片与源文件同名放一起。
- 二进制文件（docx/xlsx/drawio/png）无法做行级 diff：修改已评审版本时**另存新版本文件**或在文档内维护版本记录，不要静默覆盖。

### Git 与基线

- 本仓库同时是配置库：需求、设计、发布都要形成**基线**（tag，如 `baseline/requirements-v1.0`）并登记基线清单。
- 已进入基线的文档，修改必须走配置经理的变更流程（变更申请→评审→更新基线），**不要直接覆盖或重写**。
- 分支约定：`main`（只进基线内容）+ `feature/<主题>` + `fix/<缺陷编号>`。
- 提交信息格式：`feat|fix|docs|test|model: 摘要`；涉及缺陷时引用 Gitee issue 编号，如 `fix: 修复并发预约重复提交 (#23)`。
- 不提交 Office 临时文件（`~$*`）、`.DS_Store` 等，`.gitignore` 已排除。

### 红线

- 不要改动 `docs/course/` 下文档的**内容**（课程规定的固定格式与名单）。
- 不要绕过变更流程改写他人负责的已评审文档。
- 本校具体审批流程与资源管理规则**尚未经需求调研核实**（选题说明明确要求在需求调研阶段核实）：写需求/设计时不要臆造规则，先与系统分析员确认。

## 8. 常见任务指引

| 任务 | 做法 |
|---|---|
| 新增/修改需求文档 | 放 `docs/requirements/`，同步更新需求追踪记录与飞书任务看板 |
| 画/改 UML 图 | 改 `models/` 下 `.drawio` 源文件，导出图片一起提交 |
| 导出 Figma 原型 | 按迭代导出到 `docs/prototypes/<迭代名>/`，附评审记录 |
| 修缺陷 | Gitee 登记 → 建 `fix/<编号>` 分支 → 提交信息引用编号 → 回归后关闭 issue |
| 建基线 | 配置经理执行：打 tag `baseline/<阶段>-v<版本>` 并登记基线清单 |
| 查岗位任务/分工 | 读 `docs/planning/task-assignments.md`（任务分解基线）；日常执行状态看飞书任务看板 |
| 更新本文件 | 重大变化（技术栈确定、交付物完成、流程调整）时更新 §9 状态表与对应章节 |

## 9. 当前状态与未决事项（截至 2026-09-25）

- ✅ 选题确定、角色分工完成、工具选型完成（见 `docs/course/`）
- ✅ 岗位任务分配表 v0.1 已建立（`docs/planning/task-assignments.md`）
- ✅ 仓库结构指南 v0.1 已建立（`docs/configuration-management/repo-structure.md`）
- 交付物进度：以 `docs/planning/task-assignments.md` §0 为唯一状态源（不在此重复跟踪）
- ⬜ 技术栈未定（确定后更新本文件与 `src/`、`tests/`）
- ⬜ 本校具体审批流程及资源管理规则待需求调研核实
- ✅ git 仓库已初始化，首次提交并打 `baseline/init-v1.0` tag（2026-09-25）
- ⬜ 关联 GitHub 远程（配置经理负责）；`baseline/init-v1.0` 需在《配置管理计划》的基线清单中补登
